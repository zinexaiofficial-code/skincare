const {chromium}=require('/private/tmp/sashwari-qa/node_modules/playwright');const fs=require('fs');const path=require('path');
(async()=>{const b=await chromium.launch({channel:'chrome',headless:true});const p=await b.newPage({viewport:{width:1600,height:1200}});for(const w of [1440,1024,768,430,375]){const files=fs.readdirSync('qa/screenshots').filter(f=>f.startsWith(w+'-'));if(files.length<11)continue;const html=`<html><body style="margin:0;background:#eee;font:16px Arial"><div style="display:grid;grid-template-columns:repeat(4,1fr);gap:12px">${files.map(f=>`<div><h3>${f}</h3><img style="width:100%;display:block" src="screenshots/${f}"></div>`).join('')}</div></body></html>`;fs.writeFileSync(`qa/sheet-${w}.html`,html);await p.goto('file://'+path.resolve(`qa/sheet-${w}.html`));await p.screenshot({path:`qa/sheet-${w}.png`,fullPage:true});}await b.close()})()
